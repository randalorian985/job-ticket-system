FROM node:20.19-bullseye
WORKDIR /app
# Install dependencies during image build to have npm available
COPY package*.json ./
RUN npm ci --no-audit --silent
COPY . .
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
